{
  "phases": {
    "install": {
      "cmds": ["dotnet restore SeleafAPI.csproj"]
    },
    "build": {
      "cmds": ["dotnet publish SeleafAPI.csproj --no-restore -c Release -o out"]
    }
  },
  "start": {
    "cmd": "./out/SeleafAPI"
  }
}
